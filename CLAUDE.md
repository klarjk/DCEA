# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 이 저장소의 정체

코드가 아니라 **Claude Code 스킬 하나**(`dcea`, 커피 추출 진단)의 소스다. 빌드·테스트·린트·의존성이 없다.

`~/.claude/skills/dcea` → 이 디렉토리 심볼릭 링크. **여기를 고치면 설치된 스킬이 즉시 바뀐다.** 별도 배포 단계 없음. git 저장소가 아니므로 되돌릴 이력이 없다 — 파일 3개 이상을 옮기거나 지우는 작업, 디렉토리 구조 변경 전에는 전체를 스크래치패드에 복사해 둔다.

동작 확인은 `/dcea` 또는 추출 관련 발화로 스킬을 실제 호출해 본다.

## 3계층 구조

| 계층 | 파일 | 로드 시점 | 역할 |
|---|---|---|---|
| 절차 | `SKILL.md` | 스킬 호출 시 항상 | 입력 수집 → 향미 총량 판정 → 진단 → 처방의 순서, 답변 형식, 참조 라우팅 |
| 지식 | `references/*.md` | 필요할 때만 | 이론·진단표·변수 사전·레시피·기구별 메커니즘 |
| 근거 | `sources/blog/*.txt` | 원문 확인 시만 | 블로그 저자(klar201) 원문 18편 |

`SKILL.md`는 **얇게 유지한다.** 지식을 본문에 늘리지 말고 `references/`에 넣고 라우팅 표에서 가리킨다. 반대로 절차·형식 규칙은 `references/`에 흩지 않는다.

각 참조 문서는 담당 주제가 하나다 — `model.md` 이론, `diagnosis.md` 관능→원인→처방, `variables.md` 변수별 방향, `recipes.md` 레시피 프리셋, `brewers/` 기구 메커니즘, `pitfalls.md` 금지 경로·통념 충돌·저자 미지 영역, `profile-template.md` 사용자 프로필 형식·기록 규칙. 같은 내용을 두 곳에 쓰지 말고 한 곳에 두고 다른 쪽에서 파일명으로 참조한다.

`references/` 파일을 추가·삭제·개명하면 `SKILL.md` 하단 "참조 문서" 표를 함께 고친다.

사용자 프로필(장비·기준선·진단 이력)은 이 저장소가 아니라 **`~/.claude/dcea/profile.md`**에 쌓인다. 심볼릭 링크로 설치되므로 저장소 안에 두면 배포 시 남의 기록이 여기 쌓인다. 형식은 `references/profile-template.md`, 읽기·쓰기 규칙은 `SKILL.md`의 0단계와 5단계.

프로필의 기준선은 **사용자 실측값이므로 저자 수치보다 우선한다.** 이 우선순위를 뒤집지 않는다.

## 출처·확신도 표기

- **출처**: `(원문 NN)` = `sources/blog/NN_*.txt`. 번호↔제목 대응은 `sources/blog/_index.txt`.
- **04번 원문은 파일이 없다**(스크랩 글이라 본문 미수집). 인용 금지.
- **17번(Sprover)도 파일이 없다.** 특수 추출이라 진단 범위 밖으로 두어 수집하지 않았다. 인용 금지.
- **18번(프리인퓨전 탐구)은 블로그에 카페 링크만 있어 저자 원고를 본문으로 등재했다** — 인용 가능.
- **확신도**: `[검증]` 직접 실험 / `[경험]` 경험적 관찰 / `[이론]` 물리 유도 / `[가설]` 저자 추정.

내용을 파일 간에 옮기거나 요약할 때 두 표기를 반드시 함께 옮긴다. 확신도가 낮은 항목을 단정형으로 승격하지 않는다.

> **Do:** "떫음은 유속 순간 최대값 과다 [가설] (원문 01)"을 다른 문서로 옮기며 표기를 그대로 유지한다
> **Don't:** 옮기면서 `[가설]`을 떼거나 "(원문 01)"을 생략한다

## 편집 시 깨뜨리면 안 되는 것

이 스킬의 존재 이유가 일반 커피 상식과의 충돌이다. 아래는 문서를 손볼 때 실수로 "정상화"되기 쉬운 항목이다.

- **금지 경로** — 향미가 부족할 때 유량↑·시간↑·분쇄 가늘게로 "더 뽑아내는" 처방을 만들지 않는다. 반대로 유량↓·TDS↑가 저자 처방이다.
- **떫음 ≠ 풋내** — 떫음은 유속의 순간 최대값, 풋내는 평균 유량. 통념과 달리 둘 다 과다추출 쪽이며 조작 대상이 다르다. 하나로 합치지 않는다.
- **변수 2계층** — 처방은 2계층(온도·유량·추출비·분쇄도)에서만 나간다. 1계층(로스팅·그라인더·도징량·디개싱)은 파악용. 단 `SKILL.md`의 "도징량은 향미 총량 조정 목적이면 예외" 조항은 모순으로 오인해 지우지 않는다.
- **절대값 금지** — 모든 수치는 저자 장비(Lagom P64 + SSP cast, Decent XL, V60) 기준이다. "지난번보다 어느 쪽으로"로 표현하고, 수치를 인용하면 장비 기준임을 명시한다. 예외는 프로필의 기준선뿐이다 — 그 사용자가 2회 이상 확인해준 실측값이므로 절대값으로 써도 된다.
- **저자가 모른다고 밝힌 것**(`pitfalls.md`)은 채워 넣지 않는다.

새 지식을 넣기 전에 `references/pitfalls.md`를 읽어 기존 항목과 충돌하지 않는지 확인한다. 충돌하면 임의로 정하지 말고 사용자에게 묻는다.

## 답변 분량 규칙

`SKILL.md`가 처방 답변 300단어·되묻는 답변 150단어 상한을 건다. 진단 로직을 보강할 때 이 상한을 초과하게 만드는 필수 출력 항목을 추가하지 않는다.
