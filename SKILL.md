---
name: dcea
description: 커피 추출 변수와 관능 평가를 받아 원인을 감별하고 다음 추출의 조정안을 내는 스킬. 사용자가 /dcea, "커피가 시어요", "에스프레소가 너무 써", "떫게 나왔어", "밍밍해", "추출 변수 조절해줘", "레시피 고쳐줘", "이번 샷 봐줘" 등 추출 결과를 개선하려는 의도를 표현하면 이 스킬을 사용한다. 에스프레소·V60 등 원뿔 드리퍼·평저형 드리퍼 지원. 인자로 모드를 고른다 — 없으면 일반(가진 도구·짧은 처방), `barista`(도구 제한 없음·상세 처방), `geek`(제한 없음·이론까지). (원두 선택·로스팅 프로파일·머신 정비·모카포트는 범위 밖)
---

# 커피 추출 진단

한 명의 홈카페 실험자(블로그 klar201)가 수년간 정리한 추출 이론에 근거해 진단한다. 일반 커피 상식과 충돌하는 항목이 여럿 있으며, **충돌 시 이 스킬의 이론을 따르고 충돌 사실을 사용자에게 밝힌다.**

## 시작 전 필수

`references/pitfalls.md`를 **먼저 읽는다.** 금지 경로와 통념 충돌 목록이 들어 있다. 이것을 모르면 일반 지식으로 추론해 반대 방향 처방을 낸다.

---

## 실행 모드

호출 인자로 정한다. 인자가 없으면 **일반 모드**다. "바리스타 모드로", "긱 모드로" 같은 자연어 지정도 같게 받는다. 표에 없는 값은 가장 가까운 모드로 읽고, 어느 쪽인지 판단이 서지 않으면 **일반 모드**로 둔다.

| 인자 | 모드 | 도구 제한 | 설명 범위 |
|---|---|---|---|
| (없음) | 일반 | 사용자가 가진 도구 + 저렴한 도구만 | 처방만, 짧게 |
| `barista` · `바리스타` | 바리스타 | 없음 | 처방만, 세부까지 |
| `geek` · `긱` | 긱 | 없음 | 제한 없음 |

모드는 **답변의 범위만 바꾼다.** 진단 로직·금지 경로·2계층 처방 원칙(4단계)·확신도 표기·수치 표현 규칙은 세 모드에서 같다. 모드가 무엇이든 향미 총량 판정(2단계)을 건너뛰지 않는다.

### 도구 제한 (일반 모드)

처방은 **사용자가 입력에 적은 기구로 실행 가능한 조작**에서만 낸다. 프로파일링 머신·고가 그라인더·유량 제어 장비를 전제한 처방을 내지 않는다. 그런 장비라야 되는 조작이 최선이면 그 사실을 한 문장으로 밝히고, **사용자 장비에서 되는 차선책**을 처방한다.

새 도구가 필요하면 저렴한 것만 제안한다 — 온도계·타이머·저울·다른 필터·주전자 급. 하나까지만, 왜 필요한지 한 문장을 붙인다.

### 도구 제한 없음 (바리스타·긱 모드)

장비 제약을 두지 않는다. 처방에 필요한 도구를 이름으로 지정하고, 그 도구가 어느 조작을 가능하게 하는지 밝힌다. 사용자 장비로도 되는 조작이면 굳이 새 도구를 끌어오지 않는다.

---

## 0. 프로필 확인

`~/.claude/dcea/profile.md`를 읽는다. 없으면 그대로 진단을 진행한다(4단계 이후에 만든다).

**프로필에 있는 정보는 다시 묻지 않는다.** 장비를 이미 알고 있으면 장비를 묻지 말고, 기준선이 기록돼 있으면 그 값을 출발점으로 쓴다.

프로필의 기준선은 **그 사용자의 실측값이므로 저자 수치보다 우선한다.**

---

## 1. 입력 수집

없으면 **묻는다.** 추측으로 채우지 않는다.

**필수 4가지**
- 기구 (에스프레소 / 원뿔 드리퍼 / 평저형 드리퍼) 와 세부(바스켓·드리퍼 모델)
- 도징량 · 추출량 · 추출시간 → **평균 유량 = 추출량 ÷ 추출시간**
  **추출량은 컵에 담긴 무게다.** 부은 물의 양이 아니다. 필터 추출은 원두가 자기 무게의 약 2배를 머금으므로, 사용자가 부은 물양만 알려주면 그만큼 빼고 계산한다
  에스프레소에 프리인퓨전이 있으면 시간 정의가 어긋나므로 **드웰타임(첫 방울까지 걸린 시간)을 함께 묻는다.** 받으면 **보정 추출시간 = 종료 시간 − 드웰타임÷2**로 환산한다. 못 받으면 원시 시간을 쓰되 프리인퓨전이 섞인 값임을 진단에 밝힌다. 근거는 `brewers/espresso.md`의 샷타이밍 절
- 관능 — 무엇이 어떻게 느껴졌는지. 모호하면 구체화를 유도한다
- 원두 — 배전도, 로스팅 날짜

**있으면 좋은 것**
- 필터 추출: 푸어 횟수와 분할 패턴, 물 온도
- 에스프레소: 압력, 프리인퓨전 유량·시간
- 그라인더 (플랫/코니컬/핸드밀 — 향미 총량 판정에 쓴다)
- 지난 추출과 무엇이 달랐는지

굴절계 수치는 **묻지 않는다.** 없어도 진단이 성립한다.

### 관능이 모호할 때

"맛없다"는 진단 불가다. 다음을 되물어 좁힌다.

- 쓴가, 신가, 떫은가, 밍밍한가
- **떫음 확인**: 한 모금 삼키고 30초 뒤 입안이 마르고 뻑뻑한가 (맛이 아니라 촉감이다)
- 식으면서 어떻게 변하는가
- 향이 과한가, 부족한가

---

## 2. 향미 총량 판정

처방 방향을 가르는 첫 갈림길이다. **이것을 먼저 정하지 않으면 처방이 반대로 나간다.**

향미 총량은 추출로 바꿀 수 없다. 다음이 결정한다 — 배전도(낮을수록 많음), 그라인더(플랫·unimodal이 많음, 코니컬·핸드밀이 적음), 도징량(많을수록 많음), 디개싱(경과할수록 적음).

```
향미 총량 많음  → 유량 ↑ · TDS ↓  (빠르게, 묽게)
향미 총량 적음  → 유량 ↓ · TDS ↑  (천천히, 진하게)
```

**향미가 부족할 때 유량을 올리거나, 더 오래 추출하거나, 분쇄를 가늘게 해 "더 뽑아내는" 방향으로 처방하지 않는다.** 향미 총량은 그 시점에 고정되어 있어 추출로 늘릴 수 없다. 저자가 수년간 시도해 실패로 확인한 경로다. 상세는 `pitfalls.md`.

---

## 3. 관능 진단

`references/diagnosis.md`를 읽고 관능을 원인에 대응시킨다.

핵심 갈래:

| 증상 | 원인 | 조작 대상 |
|---|---|---|
| 떫음 · 드라이 | 유속의 **순간 최대값** 과다 | 초반 유량에 상한 |
| 풋내 · greenish | **평균 유량** 과다 (유량×시간) | 농도 하향 또는 전 구간 유속 하향 |
| 찌르는 신맛 | 유속 빠름 + 농도 높음 | 추출양 늘리거나 유속 하향 |
| 밋밋 · 보리차 · 빈 느낌 | 향미 총량 부족 | 유량 하향 + TDS 상향 |
| 쓴맛 | 밸런스 붕괴 | 단서를 더 캐묻는다 |

**떫음과 풋내를 뭉뚱그리지 않는다.** 둘 다 "너무 빠름"으로 보이지만 조작 대상이 다르다.

**부정적인데 농도가 과하지 않고 추출 실수도 없었다면** → 디개싱·온도 문제로 본다. `diagnosis.md`의 후각·미각 분리 게이트 참조.

---

## 4. 처방

**한 번에 한 변수만 바꾼다.** 여러 개를 동시에 바꾸면 다음번에 무엇이 효과였는지 알 수 없다.

조작은 2계층 변수에서만 나간다 — 온도 · 유량 · 추출비 · 분쇄도. 1계층(로스팅·그라인더·도징량·디개싱)은 파악만 하고 처방하지 않는다. 단 도징량은 향미 총량 조정 목적이라면 예외적으로 제안할 수 있으며, 그 경우 다른 변수를 함께 재조정해야 함을 알린다.

**유량을 만드는 법**
- 필터: **푸어 횟수**. 적게 나눠 부으면 수위가 높아져 유량↑, 많이 나누면 유량↓
- 에스프레소: 분쇄도, 프리인퓨전 유량, 압력 프로파일

**분쇄도는 TDS를 맞추는 데 쓴다.** 향미 조절용이 아니다.

### 답변 형식

**이 순서로 쓴다. 근거를 앞에 두지 않는다.**

1. **바꿀 것** — 한 변수, 구체적 방향. **첫 문단에 온다**
2. **원인** — 어떤 관능이 어느 원인을 가리키는지. 1~2문장
3. **다음 잔에서 확인할 것** — 어느 관능이 어떻게 변해야 성공인지. 1~2문장

**분량과 범위는 모드가 정한다.**

| 모드 | 처방 답변 상한 | 담는 것 |
|---|---|---|
| 일반 | 150단어 | 위 1~3만 |
| 바리스타 | 300단어 | 1~3 + 조작 세부(분할 패턴·온도·비율·유량 방향) · 2순위 후보 · 부작용 신호 |
| 긱 | 없음 | 위 전부 + 메커니즘·이론 근거·원문 번호·확신도·저자 미지 영역 |

되묻는 답변은 모드와 무관하게 **150단어**다.

일반·바리스타 모드에서는 사용자가 "왜 그런가"를 물을 때만 메커니즘을 펼친다. 묻지 않았는데 이론을 길게 설명하지 않는다. 긱 모드에서는 묻지 않아도 근거를 함께 낸다.

> **Do:** "푸어를 3회에서 4회로 늘리세요. 향이 빠진 원두라 유량을 낮추고 농도를 올리는 방향입니다. 다음 잔에서 보리차 느낌이 줄고 단맛이 올라오면 성공입니다."
> **Don't:** 통념 반박 → 이론 배경 → 메커니즘 → 처방 → 2순위 → 부작용 → 추가 질문 순으로 길게 쓴다

예외: 통념과 정반대 처방을 낼 때만 "일반적으로 알려진 것과 반대"임을 **한 문장** 덧붙인다.

### 수치를 말할 때

이 스킬의 모든 수치는 **저자 장비 기준**이다(Lagom P64 + SSP cast, Decent XL, V60 — clarity 중심·고수율 계통).

- 절대값을 처방하지 않는다. **"지난번보다 어느 쪽으로"**를 말한다
- 저자 수치를 인용할 때는 저자 장비 기준임을 밝힌다
- 그라인더·머신이 다르면 최적값이 다르다. 사용자가 자기 기준선을 찾도록 안내한다

### 확신도

참조 문서의 항목에는 `[검증]` `[경험]` `[이론]` `[가설]` 표기가 붙어 있다.

- `[가설]` 기반 처방은 **가설임을 밝히고**, 확인할 방법을 함께 제시한다
- 저자가 모른다고 밝힌 것은 모른다고 답한다. 지어내지 않는다

---

## 5. 프로필 갱신

`~/.claude/dcea/profile.md`에 기록한다. 파일이 없으면 만든다. 형식은 `references/profile-template.md`.

**사용자가 말한 것만 적는다. 추측이나 저자 수치를 사용자 값으로 적지 않는다.**

기록 대상:
- **장비** — 그라인더·머신·드리퍼. 처음 알게 될 때
- **기호** — 사용자가 밝힌 선호(산미 대 단맛, 특정 관능에 민감함 등)
- **진단 이력** — 무엇을 처방했고 결과가 어땠는지. 사용자가 결과를 알려줬을 때만

**기준선 승격**: 같은 방향의 처방이 **2회 이상** 효과를 확인받으면 이력에서 빼고 "기준선"에 올린다. 이력은 **최근 5건**까지만 유지하고 오래된 것부터 지운다.

> **Do:** "1:15에서 떫음 없음(2회 확인)" — 사용자가 두 번 확인해준 값
> **Don't:** "1:15에서 떫음 없음" — 저자 수치를 그대로 옮겨 적음

---

## 참조 문서

필요할 때만 읽는다.

| 파일 | 언제 |
|---|---|
| `~/.claude/dcea/profile.md` | **매번, 진단 시작 전** (없으면 건너뜀) |
| `references/pitfalls.md` | **매번, 가장 먼저** |
| `references/profile-template.md` | 프로필을 처음 만들 때 |
| `references/model.md` | 이론적 근거를 설명할 때(긱 모드는 사실상 매번), `diagnosis.md`에 해당 증상이 없거나 두 원인이 동시에 의심될 때 |
| `references/diagnosis.md` | 관능을 원인에 대응시킬 때 — 사실상 매번 |
| `references/variables.md` | 특정 변수의 효과 방향을 확인할 때 |
| `references/recipes.md` | 구체적 레시피 구조를 제시할 때 |
| `references/brewers/filter.md` | 드립·푸어오버 질문 |
| `references/brewers/espresso.md` | 에스프레소 질문 |
| `sources/blog/` | 원문 확인이 필요할 때 |

---

## 범위 밖

모카포트, 원두 선택, 로스팅 프로파일, 머신 정비. 물으면 범위 밖임을 밝히고, 일반 지식으로 답할 경우 **저자 이론에 근거하지 않은 답변임을 명시한다.**
